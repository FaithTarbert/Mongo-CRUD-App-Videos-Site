
            <div class="container">
        <h1 class="my-4">Course title : {{video.title}}</h1>
        <div class="row">
            <div class="col-md-8">
                <img class="img-fluid"
                    src="{{video.imageUrl}}"
                    alt="Express" />
            </div>

            <div class="col-md-4 text-center">
                <h3 class="my-3">{{video.description}}</h3>
                <p>{{video.createdAt}}</p>
                {{!-- <form method="POST" action="/delete/{{video._id}}" enctype="application/x-www-form-urlencoded">
                <input type="hidden" name="_method" value="DELETE">
                <button class="btn btn-danger" type="submit">Delete</button>
                </form> --}}
                <a class="btn btn-warning" href="/">Edit</a>
                <a class="btn btn-warning" href="/delete/{{video._id}}">Delete</a>
                <span class="enrolled-span">You are already enrolled</span>
            </div>
        </div>
    </div>
